import { Box } from '@mui/material';

export const StripBar = () => {
    return (
        <Box width={'100%'} height={80} position={'fixed'} zIndex={1000} left={0} bottom={0} bgcolor={'#ECF2FF'}></Box>
    );
};
